<?xml version="1.0"?>
<!--
  config.xml — Configuração customizada do ClickHouse
  Montado em: /etc/clickhouse-server/config.d/custom.xml
  Sobrescreve apenas os campos definidos aqui (merge com config padrão).
-->
<clickhouse>
    <!-- Escuta em todas as interfaces (necessário para acesso via Docker network) -->
    <listen_host>0.0.0.0</listen_host>

    <!-- Porta HTTP (padrão: 8123) -->
    <http_port>8123</http_port>

    <!-- Porta nativa/TCP (padrão: 9000) -->
    <tcp_port>9000</tcp_port>

    <!-- Logs -->
    <logger>
        <level>warning</level>
        <console>1</console>
    </logger>

    <!-- Diretórios de dados -->
    <path>/var/lib/clickhouse/</path>
    <tmp_path>/var/lib/clickhouse/tmp/</tmp_path>
    <user_files_path>/var/lib/clickhouse/user_files/</user_files_path>

    <!-- Limite de memória: 80% da RAM disponível -->
    <max_server_memory_usage_to_ram_ratio>0.8</max_server_memory_usage_to_ram_ratio>

    <!-- Timezone padrão -->
    <timezone>America/Sao_Paulo</timezone>
</clickhouse>
